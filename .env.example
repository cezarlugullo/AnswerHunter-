# AnswerHunter - API Keys Template
# Copy this file to .env and fill in your own keys.
# The .env file is NOT committed to git (see .gitignore).
#
# Keys are entered directly in the extension's settings UI —
# this file is only used for local development/testing scripts.

# Groq API key (required) — get yours at https://console.groq.com
GROQ_API_KEY=

# Serper API key (optional, for Google search) — get yours at https://serper.dev
SERPER_API_KEY=

# Google Gemini API key (optional) — get yours at https://aistudio.google.com
GEMINI_API_KEY=
